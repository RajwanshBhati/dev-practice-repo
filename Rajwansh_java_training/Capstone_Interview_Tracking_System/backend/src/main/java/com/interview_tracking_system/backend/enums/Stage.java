package com.interview_tracking_system.backend.enums;

/**
 * Enum representing the different stages of the interview process.
 */
public enum Stage {
    PROFILING,
    SCREENING,
    L1,
    L2,
    HR,
    REJECTED,
    SELECTED
}