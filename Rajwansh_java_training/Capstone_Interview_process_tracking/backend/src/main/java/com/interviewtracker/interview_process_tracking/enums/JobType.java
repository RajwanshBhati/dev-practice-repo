package com.interviewtracker.interview_process_tracking.enums;

// Here I have defined the JobType enum with three possible values: FULL_TIME, CONTRACT, and REMOTE. This enum will be used to represent the type of job in the application.
public enum JobType {
    FULL_TIME,
    CONTRACT,
    REMOTE
}